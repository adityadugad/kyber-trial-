services:
  - type: web
    name: kyber-trial
    env: python
    pythonVersion: 3.10
    # Run build.sh using bash so it works even if not executable
    buildCommand: bash build.sh
    startCommand: gunicorn app:app --bind 0.0.0.0:10000
    autoDeploy: true
